name: architecture
description: Complex architecture, refactor, or multi-file workflow.
stages:
  - main
  - router
  - scout
  - analyst
  - hermes_reviewer
  - analyst_revision
  - guard
  - executor
  - verifier
  - memory
execution:
  writes_project_files: true
  requires_guard: true
  requires_verifier: true
  requires_hermes: true

